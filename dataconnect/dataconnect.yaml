specVersion: "v1beta"
serviceId: "snapsync"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "snapsync-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
